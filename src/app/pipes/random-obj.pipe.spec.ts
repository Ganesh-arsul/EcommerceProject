import { RandomObjPipe } from './random-obj.pipe';

describe('RandomObjPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomObjPipe();
    expect(pipe).toBeTruthy();
  });
});
